#include <iostream>

int main() {
    unsigned n, m, x;
	std::cin >> n >> m;
    bool room[10000] = {false};
    for(unsigned i = 0; i < n; i++)
        std::cin >> room[i];
    for(unsigned i = 0; i < m; i++) {
        std::cin >> x;
        for(unsigned j = 0; j < x; j++) {
            
        }
    }
}
